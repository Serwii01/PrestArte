# Permite que Lombok copie estas anotaciones a los parámetros del constructor
# generado por @RequiredArgsConstructor / @AllArgsConstructor.
# Necesario para que @Lazy funcione con inyección por constructor.
lombok.copyableAnnotations += org.springframework.context.annotation.Lazy
config.stopBubbling = true

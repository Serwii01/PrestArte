export interface TransportCompanyResponse {
  id: number;
  email: string;
  name: string;
  phone?: string;
  taxId?: string;
  companyName: string;
  coverageArea?: string;
  contactEmail?: string;
}
